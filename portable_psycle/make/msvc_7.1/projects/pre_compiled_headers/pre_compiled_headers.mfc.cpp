#include <stdafx.h>
#pragma hdrstop // borland and msvc: instructs the compiler to stop pre-compiling headers
void attribute(dllexport) pre_compiled_headers() {} // a dummy function needed to force msvc to generate something
